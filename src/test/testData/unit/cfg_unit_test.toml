source = """fn usag<caret>e() {}

fn main() {
    usage();
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn my_test() {
        usage();
    }
}"""
expected = [
    { position = { line = 4, column = 5 }, visible = true },
    { position = { line = 12, column = 9 }, visible = false }
]